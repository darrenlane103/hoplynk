# Required: Path to the dataset JSON file (relative to apps/backend directory)
DATA_FILE=data/network_data.json

# Optional: Backend server port (default: 3001)
PORT=3001

# Optional: Allowed CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000

# Optional: Node environment (development, production, test)
NODE_ENV=development
